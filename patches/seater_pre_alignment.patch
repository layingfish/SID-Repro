diff --git a/config/const.py b/config/const.py
index e341224..f90e072 100644
--- a/config/const.py
+++ b/config/const.py
@@ -76,3 +76,103 @@ class MIND_Config(object):
         # experiment const config
         self.reco_his_max_length = 20 
 
+
+
+class SETRec_beauty_Config(object):
+    def __init__(self) -> None:
+
+        # data file path
+        self.datafile_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/beauty"
+        self.training_file = "dataset/training.tsv"
+        self.validation_file = "dataset/validation.tsv"
+        self.test_file = "dataset/test.tsv"
+        self.itemID_2_attr = "vocab/item_2_attr_mapping.npy"
+
+        # for tree-based index
+        self.tree_data_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/beauty/tree_data_SASREC"
+        self.tree_based_itemID_2_indexID = "itemID_2_tree_indexID.npy"
+        self.tree_based_prefix_tree = "tree_node_allowed_next_tokens.npy"
+
+        self.two_tower_item_emb = "vocab/beauty_SASREC_item_emb.npy"
+
+        # dataset config
+        self.users_ID_num = 18136
+        self.item_ID_num = 12024 + 1 # zero for padding
+
+        # experiment const config
+        self.reco_his_max_length = 50
+
+
+class SETRec_toys_Config(object):
+    def __init__(self) -> None:
+
+        # data file path
+        self.datafile_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/toys"
+        self.training_file = "dataset/training.tsv"
+        self.validation_file = "dataset/validation.tsv"
+        self.test_file = "dataset/test.tsv"
+        self.itemID_2_attr = "vocab/item_2_attr_mapping.npy"
+
+        # for tree-based index
+        self.tree_data_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/toys/tree_data_SASREC"
+        self.tree_based_itemID_2_indexID = "itemID_2_tree_indexID.npy"
+        self.tree_based_prefix_tree = "tree_node_allowed_next_tokens.npy"
+
+        self.two_tower_item_emb = "vocab/toys_SASREC_item_emb.npy"
+
+        # dataset config
+        self.users_ID_num = 15635
+        self.item_ID_num = 11908 + 1 # zero for padding
+
+        # experiment const config
+        self.reco_his_max_length = 50
+
+
+class SETRec_sports_Config(object):
+    def __init__(self) -> None:
+
+        # data file path
+        self.datafile_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/sports"
+        self.training_file = "dataset/training.tsv"
+        self.validation_file = "dataset/validation.tsv"
+        self.test_file = "dataset/test.tsv"
+        self.itemID_2_attr = "vocab/item_2_attr_mapping.npy"
+
+        # for tree-based index
+        self.tree_data_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/sports/tree_data_SASREC"
+        self.tree_based_itemID_2_indexID = "itemID_2_tree_indexID.npy"
+        self.tree_based_prefix_tree = "tree_node_allowed_next_tokens.npy"
+
+        self.two_tower_item_emb = "vocab/sports_SASREC_item_emb.npy"
+
+        # dataset config
+        self.users_ID_num = 27591
+        self.item_ID_num = 18340 + 1 # zero for padding
+
+        # experiment const config
+        self.reco_his_max_length = 50
+
+
+class SETRec_steam_Config(object):
+    def __init__(self) -> None:
+
+        # data file path
+        self.datafile_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/steam"
+        self.training_file = "dataset/training.tsv"
+        self.validation_file = "dataset/validation.tsv"
+        self.test_file = "dataset/test.tsv"
+        self.itemID_2_attr = "vocab/item_2_attr_mapping.npy"
+
+        # for tree-based index
+        self.tree_data_par_path = "/data/xqp_data/RecSys26/datasets/seater_setrec/steam/tree_data_SASREC"
+        self.tree_based_itemID_2_indexID = "itemID_2_tree_indexID.npy"
+        self.tree_based_prefix_tree = "tree_node_allowed_next_tokens.npy"
+
+        self.two_tower_item_emb = "vocab/steam_SASREC_item_emb.npy"
+
+        # dataset config
+        self.users_ID_num = 32919
+        self.item_ID_num = 14330 + 1 # zero for padding
+
+        # experiment const config
+        self.reco_his_max_length = 50
diff --git a/main.py b/main.py
index 5e944a8..dab0349 100644
--- a/main.py
+++ b/main.py
@@ -45,13 +45,27 @@ setup_seed(args.random_seed)
 
 # update paremeter w.r.t dataset
 dataset_paras = {
-    'Yelp': {'batch_size': 256, 'test_batch_size': 1024, 'epochs': 50},
-    'MIND': {'batch_size': 256, 'test_batch_size': 1024, 'epochs': 50},
-    'Books': {'batch_size': 2048, 'test_batch_size': 2048, 'epochs': 60},
+    "Yelp": {"batch_size": 256, "test_batch_size": 1024, "epochs": 50},
+    "MIND": {"batch_size": 256, "test_batch_size": 1024, "epochs": 50},
+    "Books": {"batch_size": 2048, "test_batch_size": 2048, "epochs": 60},
+    # SETRec domains (generated under /data/xqp_data/RecSys26/datasets/seater_setrec/)
+    "SETRec_beauty": {"batch_size": 256, "test_batch_size": 1024, "epochs": 50},
+    "SETRec_toys": {"batch_size": 256, "test_batch_size": 1024, "epochs": 50},
+    "SETRec_sports": {"batch_size": 256, "test_batch_size": 1024, "epochs": 50},
+    "SETRec_steam": {"batch_size": 256, "test_batch_size": 1024, "epochs": 50},
 }
-if args.batch_size == 0 : args.batch_size = dataset_paras[args.dataset_name]['batch_size'] 
-if args.test_batch_size == 0: args.test_batch_size = dataset_paras[args.dataset_name]['test_batch_size'] 
-if args.epochs == 0: args.epochs = dataset_paras[args.dataset_name]['epochs'] 
+if args.dataset_name not in dataset_paras:
+    raise KeyError(
+        f"Unknown dataset_name={args.dataset_name!r}. "
+        f"Known: {sorted(dataset_paras.keys())}. "
+        f"You can also set --batch_size/--test_batch_size/--epochs to non-zero and patch main.py to bypass defaults."
+    )
+if args.batch_size == 0:
+    args.batch_size = dataset_paras[args.dataset_name]["batch_size"]
+if args.test_batch_size == 0:
+    args.test_batch_size = dataset_paras[args.dataset_name]["test_batch_size"]
+if args.epochs == 0:
+    args.epochs = dataset_paras[args.dataset_name]["epochs"]
 
 new_config = {} if args.new_config == '' else eval(args.new_config)
 cur_hyper_paras = [(k ,v) for k,v in new_config.items()]
diff --git a/utils/data.py b/utils/data.py
index e6153a0..6c37a7d 100644
--- a/utils/data.py
+++ b/utils/data.py
@@ -223,7 +223,16 @@ def worker_init_fn(worker_id):
     set_seed(GLOBAL_SEED + worker_id)
 
 def get_dataloader(data_set, bs, **kwargs):
-    return DataLoader(  data_set, batch_size = bs,
-                        pin_memory = True, 
-                        worker_init_fn=worker_init_fn, **kwargs
-                    )
\ No newline at end of file
+    num_workers = int(kwargs.get("num_workers") or 0)
+    if num_workers == 0:
+        # torch DataLoader forbids prefetch_factor/persistent_workers when num_workers == 0
+        kwargs.pop("prefetch_factor", None)
+        kwargs.pop("persistent_workers", None)
+
+    return DataLoader(
+        data_set,
+        batch_size=bs,
+        pin_memory=True,
+        worker_init_fn=worker_init_fn,
+        **kwargs,
+    )
