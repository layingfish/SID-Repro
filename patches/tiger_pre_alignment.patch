diff --git a/data/amazon.py b/data/amazon.py
index d102dc3..292d4d1 100644
--- a/data/amazon.py
+++ b/data/amazon.py
@@ -128,7 +128,11 @@ class AmazonReviews(InMemoryDataset, PreprocessingMixin):
             axis=1
         )
         
-        item_emb = self._encode_text_feature(sentences)
+        precomputed_emb_path = os.path.join(self.raw_dir, self.split, "item_emb.npy")
+        if os.path.exists(precomputed_emb_path):
+            item_emb = torch.from_numpy(np.load(precomputed_emb_path)).to(torch.float32)
+        else:
+            item_emb = self._encode_text_feature(sentences)
         data['item'].x = item_emb
         data['item'].text = np.array(sentences)
 
diff --git a/data/preprocessing.py b/data/preprocessing.py
index beb8a5a..c5779b5 100644
--- a/data/preprocessing.py
+++ b/data/preprocessing.py
@@ -39,8 +39,8 @@ class PreprocessingMixin:
     @staticmethod
     def _encode_text_feature(text_feat, model=None):
         if model is None:
-            model = SentenceTransformer('sentence-transformers/sentence-t5-xxl')
-        embeddings = model.encode(batch_size=2, sentences=text_feat, show_progress_bar=True, convert_to_tensor=True).cpu()
+            model = SentenceTransformer('sentence-transformers/sentence-t5-base')
+        embeddings = model.encode(batch_size=32, sentences=text_feat, show_progress_bar=True, convert_to_tensor=True).cpu()
         return embeddings
     
     @staticmethod
@@ -69,18 +69,21 @@ class PreprocessingMixin:
     def _ordered_train_test_split(df, on, train_split=0.8):
         threshold = df.select(pl.quantile(on, train_split)).item()
         return df.with_columns(is_train=pl.col(on) <= threshold)
-    
     @staticmethod
     def _df_to_tensor_dict(df, features):
-        out = {
-            feat: torch.from_numpy(
-                rearrange(
-                    df.select(feat).to_numpy().squeeze().tolist(), "b d -> b d"
-                )
-            ) if df.select(pl.col(feat).list.len().max() == pl.col(feat).list.len().min()).item()
-            else df.get_column("itemId").to_list()
-            for feat in features
-        }
+        out = {}
+        for feat in features:
+            col = df.get_column(feat)
+            if isinstance(col.dtype, (pl.List, pl.Array)):
+                values = col.to_list()
+                try:
+                    out[feat] = torch.from_numpy(np.stack(values))
+                except Exception:
+                    # Fallback for ragged lists
+                    out[feat] = values
+            else:
+                out[feat] = torch.from_numpy(df.select(feat).to_numpy())
+
         fut_out = {
             feat + FUT_SUFFIX: torch.from_numpy(
                 df.select(feat + FUT_SUFFIX).to_numpy()
diff --git a/data/processed.py b/data/processed.py
index b461a99..ec8d193 100644
--- a/data/processed.py
+++ b/data/processed.py
@@ -63,7 +63,10 @@ class ItemData(Dataset):
         elif train_test_split == "all":
             filt = torch.ones_like(raw_data.data["item"]["x"][:,0], dtype=bool)
 
-        self.item_data, self.item_text = raw_data.data["item"]["x"][filt], raw_data.data["item"]["text"][filt]
+        self.item_data = raw_data.data["item"]["x"][filt]
+        self.item_text = None
+        if "text" in raw_data.data["item"]:
+            self.item_text = raw_data.data["item"]["text"][filt]
 
     def __len__(self):
         return self.item_data.shape[0]
diff --git a/evaluate/metrics.py b/evaluate/metrics.py
index 0c59926..8f7c366 100644
--- a/evaluate/metrics.py
+++ b/evaluate/metrics.py
@@ -1,4 +1,6 @@
 from collections import defaultdict
+
+import torch
 from einops import rearrange
 from torch import Tensor
 
@@ -10,22 +12,36 @@ class TopKAccumulator:
 
     def reset(self):
         self.total = 0
-        self.metrics = defaultdict(int)
+        self.metrics = defaultdict(float)
 
     def accumulate(self, actual: Tensor, top_k: Tensor) -> None:
+        # actual: [B, D]
+        # top_k: [B, K, D]
         B, D = actual.shape
-        pos_match = (rearrange(actual, "b d -> b 1 d") == top_k)
+        pos_match = rearrange(actual, "b d -> b 1 d") == top_k
+
         for i in range(D):
-            match_found, rank = pos_match[...,:i+1].all(axis=-1).max(axis=-1)
+            # Slice hit: whether the first (i+1) digits match.
+            match_found, rank = pos_match[..., : i + 1].all(axis=-1).max(axis=-1)
             matched_rank = rank[match_found]
+
             for k in self.ks:
-                self.metrics[f"h@{k}_slice_:{i+1}"] += len(matched_rank[matched_rank < k])
-            
-            match_found, rank = pos_match[...,i:i+1].all(axis=-1).max(axis=-1)
+                hits = matched_rank[matched_rank < k]
+                self.metrics[f"h@{k}_slice_:{i+1}"] += float(hits.numel())
+                if hits.numel() > 0:
+                    self.metrics[f"ndcg@{k}_slice_:{i+1}"] += float(
+                        (1.0 / torch.log2(hits.float() + 2.0)).sum().item()
+                    )
+
+            # Position hit: whether the digit at position i matches.
+            match_found, rank = pos_match[..., i : i + 1].all(axis=-1).max(axis=-1)
             matched_rank = rank[match_found]
             for k in self.ks:
-                self.metrics[f"h@{k}_pos_{i}"] += len(matched_rank[matched_rank < k])
+                self.metrics[f"h@{k}_pos_{i}"] += float((matched_rank < k).sum().item())
+
         self.total += B
-        
+
     def reduce(self) -> dict:
-        return {k: v/self.total for k, v in self.metrics.items()}
+        if self.total == 0:
+            return {}
+        return {k: v / self.total for k, v in self.metrics.items()}
diff --git a/modules/model.py b/modules/model.py
index b6cbeb3..300e98b 100644
--- a/modules/model.py
+++ b/modules/model.py
@@ -243,7 +243,7 @@ class EncoderDecoderRetrievalModel(nn.Module):
             log_probas=log_probas.squeeze()
         )
             
-    @torch.compile
+    @torch.compiler.disable
     def forward(self, batch: TokenizedSeqBatch) -> ModelOutput:
         seq_mask = batch.seq_mask
         B, N = seq_mask.shape
diff --git a/modules/rqvae.py b/modules/rqvae.py
index 10a33d3..15d6a4a 100644
--- a/modules/rqvae.py
+++ b/modules/rqvae.py
@@ -137,7 +137,7 @@ class RqVae(nn.Module, PyTorchModelHubMixin):
             quantize_loss=quantize_loss
         )
 
-    @torch.compile(mode="reduce-overhead")
+    @torch.compiler.disable
     def forward(self, batch: SeqBatch, gumbel_t: float) -> RqVaeComputedLosses:
         x = batch.x
         quantized = self.get_semantic_ids(x, gumbel_t)
diff --git a/modules/tokenizer/semids.py b/modules/tokenizer/semids.py
index bba53da..6bfb370 100644
--- a/modules/tokenizer/semids.py
+++ b/modules/tokenizer/semids.py
@@ -120,7 +120,10 @@ class SemanticIdTokenizer(nn.Module):
         return out
     
     def _tokenize_seq_batch_from_cached(self, ids: Tensor) -> Tensor:
-        return rearrange(self.cached_ids[ids.flatten(), :], "(b n) d -> b (n d)", n=ids.shape[1])
+        # `cached_ids` contains an extra dedup column appended in `precompute_corpus_ids`,
+        # whose values can exceed the VQ codebook size. For decoder training we only
+        # use the original semantic ID digits (first `self.sem_ids_dim` columns).
+        return rearrange(self.cached_ids[ids.flatten(), : self.sem_ids_dim], "(b n) d -> b (n d)", n=ids.shape[1])
     
     @torch.no_grad
     @eval_mode
@@ -135,7 +138,7 @@ class SemanticIdTokenizer(nn.Module):
             seq_mask, sem_ids_fut = None, None
         else:
             B, N = batch.ids.shape
-            _, D = self.cached_ids.shape
+            D = self.sem_ids_dim
             sem_ids = self._tokenize_seq_batch_from_cached(batch.ids)
             seq_mask = batch.seq_mask.repeat_interleave(D, dim=1)
             sem_ids[~seq_mask] = -1
