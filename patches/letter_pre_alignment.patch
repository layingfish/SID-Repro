diff --git a/LETTER-TIGER/finetune.py b/LETTER-TIGER/finetune.py
index 6f26272..95d279b 100644
--- a/LETTER-TIGER/finetune.py
+++ b/LETTER-TIGER/finetune.py
@@ -1,7 +1,8 @@
 import argparse
 import os
 
-os.environ["CUDA_VISIBLE_DEVICES"] = "0,1"
+if "CUDA_VISIBLE_DEVICES" not in os.environ:
+    os.environ["CUDA_VISIBLE_DEVICES"] = "0,1"
 import sys
 from typing import List
 from transformers import EarlyStoppingCallback
@@ -45,7 +46,6 @@ def train(args):
 
     train_data, valid_data = load_datasets(args)
     add_num = tokenizer.add_tokens(train_data.datasets[0].get_new_tokens())
-    config.vocab_size = len(tokenizer)
     if local_rank == 0:
         print("add {} new token.".format(add_num))
         print("data num:", len(train_data))
@@ -56,7 +56,7 @@ def train(args):
 
 
     collator = Collator(args, tokenizer)
-    model = LETTER(config)
+    model = LETTER.from_pretrained(args.base_model, config=config)
     model.set_hyper(args.temperature)
     model.resize_token_embeddings(len(tokenizer))
     model.to(device)
diff --git a/LETTER-TIGER/test.py b/LETTER-TIGER/test.py
index afce9af..25b6327 100644
--- a/LETTER-TIGER/test.py
+++ b/LETTER-TIGER/test.py
@@ -1,7 +1,8 @@
 import argparse
 import json
 import os
-os.environ["CUDA_VISIBLE_DEVICES"] = "0,1,2,3"
+if "CUDA_VISIBLE_DEVICES" not in os.environ:
+    os.environ["CUDA_VISIBLE_DEVICES"] = "0,1,2,3"
 import sys
 from typing import List
 
diff --git a/RQ-VAE/datasets.py b/RQ-VAE/datasets.py
index 96b5cf1..3ee5aeb 100644
--- a/RQ-VAE/datasets.py
+++ b/RQ-VAE/datasets.py
@@ -5,17 +5,17 @@ import torch.utils.data as data
 
 class EmbDataset(data.Dataset):
 
-    def __init__(self,data_path):
-
+    def __init__(self, data_path: str):
         self.data_path = data_path
-        # self.embeddings = np.fromfile(data_path, dtype=np.float32).reshape(16859,-1)
-        self.embeddings = np.load(data_path)
-        self.dim = self.embeddings.shape[-1]
+        arr = np.load(data_path)
+        if arr.dtype != np.float32:
+            arr = arr.astype(np.float32)
+        # Keep embeddings as a CPU torch Tensor to avoid per-sample tensor allocations.
+        self.embeddings = torch.from_numpy(arr)
+        self.dim = int(self.embeddings.shape[-1])
 
-    def __getitem__(self, index):
-        emb = self.embeddings[index]
-        tensor_emb=torch.FloatTensor(emb)
-        return tensor_emb, index
+    def __getitem__(self, index: int):
+        return self.embeddings[index], index
 
-    def __len__(self):
-        return len(self.embeddings)
+    def __len__(self) -> int:
+        return int(self.embeddings.shape[0])
diff --git a/RQ-VAE/generate_indices.py b/RQ-VAE/generate_indices.py
index f275972..296dcac 100644
--- a/RQ-VAE/generate_indices.py
+++ b/RQ-VAE/generate_indices.py
@@ -98,13 +98,29 @@ all_indices_str = []
 prefix = ["<a_{}>","<b_{}>","<c_{}>","<d_{}>","<e_{}>","<f_{}>"]
 
 def constrained_km(data, n_clusters=10):
-    from k_means_constrained import KMeansConstrained 
-    # x = data.cpu().detach().numpy()
-    # data = self.embedding.weight.cpu().detach().numpy()
     x = data
-    size_min = min(len(data) // (n_clusters * 2), 10)
-    clf = KMeansConstrained(n_clusters=n_clusters, size_min=size_min, size_max=n_clusters * 6, max_iter=10, n_init=10,
-                            n_jobs=10, verbose=False)
+    try:
+        from k_means_constrained import KMeansConstrained
+        size_min = min(len(x) // (n_clusters * 2), 10)
+        clf = KMeansConstrained(
+            n_clusters=n_clusters,
+            size_min=size_min,
+            size_max=n_clusters * 6,
+            max_iter=10,
+            n_init=10,
+            n_jobs=10,
+            verbose=False,
+        )
+    except Exception:
+        from sklearn.cluster import KMeans
+
+        clf = KMeans(
+            n_clusters=n_clusters,
+            n_init=10,
+            max_iter=300,
+            random_state=42,
+        )
+
     clf.fit(x)
     t_centers = torch.from_numpy(clf.cluster_centers_)
     t_labels = torch.from_numpy(clf.labels_).tolist()
diff --git a/RQ-VAE/main.py b/RQ-VAE/main.py
index c17b701..bc0fc07 100644
--- a/RQ-VAE/main.py
+++ b/RQ-VAE/main.py
@@ -1,10 +1,11 @@
 import argparse
+import pickle
 import random
 import torch
 import numpy as np
 from time import time
 import logging
-import wandb
+# import wandb
 from torch.utils.data import DataLoader
 
 from datasets import EmbDataset
@@ -65,7 +66,33 @@ if __name__ == '__main__':
 
     print(args)
     logging.basicConfig(level=logging.DEBUG)
-    cf_emb = torch.load(args.cf_emb).squeeze().detach().numpy()
+    cf_obj = None
+    try:
+        cf_obj = torch.load(args.cf_emb)
+    except Exception:
+        with open(args.cf_emb, "rb") as f:
+            cf_obj = pickle.load(f)
+
+    try:
+        cf_obj = cf_obj.squeeze()
+    except Exception:
+        pass
+
+    if isinstance(cf_obj, torch.Tensor):
+        cf_emb = cf_obj.detach().cpu().numpy()
+    else:
+        cf_emb = np.asarray(cf_obj)
+
+    # [recsys26] Align CF embedding dim to e_dim (SETRec SASRec embeddings are often 64-d).
+    if getattr(cf_emb, "ndim", 0) == 2 and cf_emb.shape[1] != args.e_dim:
+        if cf_emb.shape[1] < args.e_dim:
+            raise ValueError(f"cf_emb dim {cf_emb.shape[1]} < e_dim {args.e_dim}")
+        try:
+            from sklearn.decomposition import PCA
+            cf_emb = PCA(n_components=args.e_dim, random_state=42).fit_transform(cf_emb)
+        except Exception:
+            cf_emb = cf_emb[:, : args.e_dim]
+    cf_emb = cf_emb.astype(np.float32, copy=False)
 
     """build dataset"""
     data = EmbDataset(args.data_path)
@@ -90,7 +117,7 @@ if __name__ == '__main__':
     print(model)
     data_loader = DataLoader(data,num_workers=args.num_workers,
                              batch_size=args.batch_size, shuffle=True,
-                             pin_memory=True)
+                             pin_memory=False)
 
     trainer = Trainer(args,model)
     best_loss, best_collision_rate = trainer.fit(data_loader)
diff --git a/RQ-VAE/models/rqvae.py b/RQ-VAE/models/rqvae.py
index a91c1cc..9ece806 100644
--- a/RQ-VAE/models/rqvae.py
+++ b/RQ-VAE/models/rqvae.py
@@ -2,7 +2,7 @@ import numpy as np
 import torch
 from torch import nn
 from torch.nn import functional as F
-import wandb
+# import wandb
 import random
 import collections
 from .layers import MLPLayers
diff --git a/RQ-VAE/models/vq.py b/RQ-VAE/models/vq.py
index 5417e30..2957514 100644
--- a/RQ-VAE/models/vq.py
+++ b/RQ-VAE/models/vq.py
@@ -3,9 +3,7 @@ import torch.nn as nn
 import torch.nn.functional as F
 from .layers import kmeans, sinkhorn_algorithm
 import random
-import wandb
-
-
+# import wandb
 class VectorQuantizer(nn.Module):
 
     def __init__(self, n_e, e_dim, mu = 0.25,
@@ -52,17 +50,35 @@ class VectorQuantizer(nn.Module):
         self.initted = True
     
     def constrained_km(self, data, n_clusters=10):
-        from k_means_constrained import KMeansConstrained 
+
         x = data.cpu().detach().numpy()
 
-        size_min = min(len(data) // (n_clusters * 2), 50) # 50 for the very first time, 10 the latter
+        try:
+            from k_means_constrained import KMeansConstrained
+
+            size_min = min(len(x) // (n_clusters * 2), 50)
+            clf = KMeansConstrained(
+                n_clusters=n_clusters,
+                size_min=size_min,
+                size_max=size_min * 4,
+                max_iter=10,
+                n_init=10,
+                n_jobs=10,
+                verbose=False,
+            )
+        except Exception:
+            from sklearn.cluster import KMeans
+
+            clf = KMeans(
+                n_clusters=n_clusters,
+                n_init=10,
+                max_iter=300,
+                random_state=42,
+            )
 
-        clf = KMeansConstrained(n_clusters=n_clusters, size_min=size_min, size_max=size_min * 4, max_iter=10, n_init=10,
-                                n_jobs=10, verbose=False) # 'size_min * 4' for the very first time, 'n_clusters * 4' for the latter
         clf.fit(x)
         t_centers = torch.from_numpy(clf.cluster_centers_)
         t_labels = torch.from_numpy(clf.labels_).tolist()
-        value_counts = {}
         return t_centers, t_labels
 
 
@@ -72,11 +88,30 @@ class VectorQuantizer(nn.Module):
 
         pos_list = [indices_list[i] for i in indices_cluster]
         pos_sample = []
-        for idx, pos in enumerate(pos_list):
-            random_element = random.choice(pos)
+        for i, pos in enumerate(pos_list):
+            self_idx = int(indices[i].item())
 
-            while random_element == indices[idx]:
+            # Avoid infinite loops when a cluster has <=1 candidate.
+            if not pos or len(pos) <= 1:
+                if self.n_e <= 1:
+                    pos_sample.append(self_idx)
+                else:
+                    r = random.randrange(self.n_e - 1)
+                    if r >= self_idx:
+                        r += 1
+                    pos_sample.append(r)
+                continue
+
+            random_element = random.choice(pos)
+            tries = 0
+            while random_element == self_idx and tries < 20:
                 random_element = random.choice(pos)
+                tries += 1
+            if random_element == self_idx:
+                r = random.randrange(self.n_e - 1)
+                if r >= self_idx:
+                    r += 1
+                random_element = r
             pos_sample.append(random_element)
 
         y_true = torch.tensor(pos_sample, device=x_q.device)
diff --git a/RQ-VAE/trainer.py b/RQ-VAE/trainer.py
index e26876e..91db0db 100644
--- a/RQ-VAE/trainer.py
+++ b/RQ-VAE/trainer.py
@@ -10,7 +10,7 @@ from tqdm import tqdm
 import torch.nn.functional as F
 from utils import ensure_dir,set_color,get_local_time
 import os
-import wandb
+# import wandb
 from datasets import EmbDataset
 from torch.utils.data import DataLoader
 
@@ -85,13 +85,29 @@ class Trainer(object):
             raise ValueError("Training loss is nan")
 
     def constrained_km(self, data, n_clusters=10):
-        from k_means_constrained import KMeansConstrained 
-        # x = data.cpu().detach().numpy()
-        # data = self.embedding.weight.cpu().detach().numpy()
         x = data
-        size_min = min(len(data) // (n_clusters * 2), 10)
-        clf = KMeansConstrained(n_clusters=n_clusters, size_min=size_min, size_max=n_clusters * 6, max_iter=10, n_init=10,
-                                n_jobs=10, verbose=False)
+        try:
+            from k_means_constrained import KMeansConstrained
+            size_min = min(len(x) // (n_clusters * 2), 10)
+            clf = KMeansConstrained(
+                n_clusters=n_clusters,
+                size_min=size_min,
+                size_max=n_clusters * 6,
+                max_iter=10,
+                n_init=10,
+                n_jobs=10,
+                verbose=False,
+            )
+        except Exception:
+            from sklearn.cluster import KMeans
+
+            clf = KMeans(
+                n_clusters=n_clusters,
+                n_init=10,
+                max_iter=300,
+                random_state=42,
+            )
+
         clf.fit(x)
         t_centers = torch.from_numpy(clf.cluster_centers_)
         t_labels = torch.from_numpy(clf.labels_).tolist()
@@ -103,7 +119,7 @@ class Trainer(object):
         original_data = EmbDataset(self.args.data_path)
         init_loader = DataLoader(original_data,num_workers=self.args.num_workers,
                              batch_size=len(original_data), shuffle=True,
-                             pin_memory=True)
+                             pin_memory=False)
         print(len(init_loader))
         iter_data = tqdm(
                     init_loader,
